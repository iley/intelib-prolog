MAKEHASHTABLE_EQ

(MAKEHASHTABLE_EQ [<ignored>])

The function creates and returns a new hash table which uses EQ
as the predicate to determine wether two keys match. 
  The optional parameter is accepted for historical reasons, 
but is ignored.

