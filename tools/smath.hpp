#ifndef INTELIB_SMATH_HPP_SENTRY
#define INTELIB_SMATH_HPP_SENTRY

#include "../sexpress/sexpress.hpp"

SReference IntelibPlus(SReference a, SReference b);
SReference IntelibMinus(SReference a, SReference b);
SReference IntelibMinus(SReference a);
SReference IntelibMult(SReference a, SReference b);
SReference IntelibDivide(SReference a, SReference b);
SReference IntelibDiv(SReference a, SReference b);
SReference IntelibMod(SReference a, SReference b);

#endif

